// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "EsteeMaster-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mssql/traits.hxx>
#include <odb/mssql/database.hxx>
#include <odb/mssql/transaction.hxx>
#include <odb/mssql/connection.hxx>
#include <odb/mssql/statement.hxx>
#include <odb/mssql/statement-cache.hxx>
#include <odb/mssql/simple-object-statements.hxx>
#include <odb/mssql/container-statements.hxx>
#include <odb/mssql/exceptions.hxx>
#include <odb/mssql/simple-object-result.hxx>

namespace odb
{
  // EsteeMaster
  //

  struct access::object_traits_impl< ::EsteeMaster, id_mssql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mssql::connection&,
      image_type&,
      id_image_type&,
      mssql::binding&,
      mssql::binding&)
    {
    }
  };

  access::object_traits_impl< ::EsteeMaster, id_mssql >::id_type
  access::object_traits_impl< ::EsteeMaster, id_mssql >::
  id (const id_image_type& i)
  {
    mssql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        id,
        i.id_value,
        static_cast<std::size_t> (i.id_size_ind),
        i.id_size_ind == SQL_NULL_DATA);
    }

    return id;
  }

  access::object_traits_impl< ::EsteeMaster, id_mssql >::id_type
  access::object_traits_impl< ::EsteeMaster, id_mssql >::
  id (const image_type& i)
  {
    mssql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        id,
        i.Estee_ID_value,
        static_cast<std::size_t> (i.Estee_ID_size_ind),
        i.Estee_ID_size_ind == SQL_NULL_DATA);
    }

    return id;
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  bind (mssql::bind* b,
        image_type& i,
        mssql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mssql;

    std::size_t n (0);

    // Estee_ID
    //
    if (sk != statement_insert && sk != statement_update)
    {
      b[n].type = mssql::bind::string;
      b[n].buffer = &i.Estee_ID_value;
      b[n].size_ind = &i.Estee_ID_size_ind;
      b[n].capacity = static_cast<SQLLEN> (sizeof (i.Estee_ID_value));
      n++;
    }

    // DataFeed_Ticker
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.DataFeed_Ticker_value;
    b[n].size_ind = &i.DataFeed_Ticker_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.DataFeed_Ticker_value));
    n++;

    // Underlying_Ticker
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Underlying_Ticker_value;
    b[n].size_ind = &i.Underlying_Ticker_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Underlying_Ticker_value));
    n++;

    // ISIN
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.ISIN_value;
    b[n].size_ind = &i.ISIN_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.ISIN_value));
    n++;

    // Exchange_Code
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Exchange_Code_value;
    b[n].size_ind = &i.Exchange_Code_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Exchange_Code_value));
    n++;

    // Security_Type
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.Security_Type_value;
    b[n].size_ind = &i.Security_Type_size_ind;
    n++;

    // Option_Type
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Option_Type_value;
    b[n].size_ind = &i.Option_Type_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Option_Type_value));
    n++;

    // Strike_Price
    //
    b[n].type = mssql::bind::float8;
    b[n].buffer = &i.Strike_Price_value;
    b[n].size_ind = &i.Strike_Price_size_ind;
    b[n].capacity = 53;
    n++;

    // Tick_Size
    //
    b[n].type = mssql::bind::float8;
    b[n].buffer = &i.Tick_Size_value;
    b[n].size_ind = &i.Tick_Size_size_ind;
    b[n].capacity = 53;
    n++;

    // Lot_Size
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.Lot_Size_value;
    b[n].size_ind = &i.Lot_Size_size_ind;
    n++;

    // Status
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Status_value;
    b[n].size_ind = &i.Status_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Status_value));
    n++;

    // Token
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.Token_value;
    b[n].size_ind = &i.Token_size_ind;
    n++;

    // TOKEN2
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.TOKEN2_value;
    b[n].size_ind = &i.TOKEN2_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.TOKEN2_value));
    n++;

    // Expiry_Date2
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Expiry_Date2_value;
    b[n].size_ind = &i.Expiry_Date2_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Expiry_Date2_value));
    n++;

    // Quote_Size
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.Quote_Size_value;
    b[n].size_ind = &i.Quote_Size_size_ind;
    n++;

    // SERIES
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.SERIES_value;
    b[n].size_ind = &i.SERIES_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.SERIES_value));
    n++;

    // Instrument_Type
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.Instrument_Type_value;
    b[n].size_ind = &i.Instrument_Type_size_ind;
    n++;

    // Source
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.Source_value;
    b[n].size_ind = &i.Source_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.Source_value));
    n++;

    // TradingCurrency
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.TradingCurrency_value;
    b[n].size_ind = &i.TradingCurrency_size_ind;
    n++;

    // SettlementCurrency
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.SettlementCurrency_value;
    b[n].size_ind = &i.SettlementCurrency_size_ind;
    n++;

    // TickSizeSlabId
    //
    b[n].type = mssql::bind::int_;
    b[n].buffer = &i.TickSizeSlabId_value;
    b[n].size_ind = &i.TickSizeSlabId_size_ind;
    n++;

    // UnderlyingEstee_Id
    //
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.UnderlyingEstee_Id_value;
    b[n].size_ind = &i.UnderlyingEstee_Id_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.UnderlyingEstee_Id_value));
    n++;

    // AsOfDate
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.AsOfDate_value;
    b[n].size_ind = &i.AsOfDate_size_ind;
    b[n].capacity = 3;
    n++;

    // Last_update_time
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.Last_update_time_value;
    b[n].size_ind = &i.Last_update_time_size_ind;
    b[n].capacity = 3;
    n++;

    // Expiry_Date
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.Expiry_Date_value;
    b[n].size_ind = &i.Expiry_Date_size_ind;
    b[n].capacity = 3;
    n++;

    // SettlementDate
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.SettlementDate_value;
    b[n].size_ind = &i.SettlementDate_size_ind;
    b[n].capacity = 3;
    n++;

    // TradingStartDate
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.TradingStartDate_value;
    b[n].size_ind = &i.TradingStartDate_size_ind;
    b[n].capacity = 3;
    n++;

    // LastTradingDate
    //
    b[n].type = mssql::bind::datetime;
    b[n].buffer = &i.LastTradingDate_value;
    b[n].size_ind = &i.LastTradingDate_size_ind;
    b[n].capacity = 3;
    n++;
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  bind (mssql::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].type = mssql::bind::string;
    b[n].buffer = &i.id_value;
    b[n].size_ind = &i.id_size_ind;
    b[n].capacity = static_cast<SQLLEN> (sizeof (i.id_value));
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  init (image_type& i,
        const object_type& o,
        mssql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mssql;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    // DataFeed_Ticker
    //
    {
      ::std::string const& v =
        o.DataFeed_Ticker;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.DataFeed_Ticker_value,
        sizeof (i.DataFeed_Ticker_value) - 1,
        size,
        is_null,
        v);
      i.DataFeed_Ticker_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Underlying_Ticker
    //
    {
      ::std::string const& v =
        o.Underlying_Ticker;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Underlying_Ticker_value,
        sizeof (i.Underlying_Ticker_value) - 1,
        size,
        is_null,
        v);
      i.Underlying_Ticker_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // ISIN
    //
    {
      ::std::string const& v =
        o.ISIN;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.ISIN_value,
        sizeof (i.ISIN_value) - 1,
        size,
        is_null,
        v);
      i.ISIN_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Exchange_Code
    //
    {
      ::std::string const& v =
        o.Exchange_Code;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Exchange_Code_value,
        sizeof (i.Exchange_Code_value) - 1,
        size,
        is_null,
        v);
      i.Exchange_Code_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Security_Type
    //
    {
      int const& v =
        o.Security_Type;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.Security_Type_value, is_null, v);
      i.Security_Type_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Option_Type
    //
    {
      ::std::string const& v =
        o.Option_Type;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Option_Type_value,
        sizeof (i.Option_Type_value) - 1,
        size,
        is_null,
        v);
      i.Option_Type_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Strike_Price
    //
    {
      double const& v =
        o.Strike_Price;

      bool is_null (false);
      mssql::value_traits<
          double,
          mssql::id_float8 >::set_image (
        i.Strike_Price_value, is_null, v);
      i.Strike_Price_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Tick_Size
    //
    {
      double const& v =
        o.Tick_Size;

      bool is_null (false);
      mssql::value_traits<
          double,
          mssql::id_float8 >::set_image (
        i.Tick_Size_value, is_null, v);
      i.Tick_Size_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Lot_Size
    //
    {
      int const& v =
        o.Lot_Size;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.Lot_Size_value, is_null, v);
      i.Lot_Size_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Status
    //
    {
      ::std::string const& v =
        o.Status;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Status_value,
        sizeof (i.Status_value) - 1,
        size,
        is_null,
        v);
      i.Status_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Token
    //
    {
      int const& v =
        o.Token;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.Token_value, is_null, v);
      i.Token_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // TOKEN2
    //
    {
      ::std::string const& v =
        o.TOKEN2;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.TOKEN2_value,
        sizeof (i.TOKEN2_value) - 1,
        size,
        is_null,
        v);
      i.TOKEN2_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Expiry_Date2
    //
    {
      ::std::string const& v =
        o.Expiry_Date2;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Expiry_Date2_value,
        sizeof (i.Expiry_Date2_value) - 1,
        size,
        is_null,
        v);
      i.Expiry_Date2_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Quote_Size
    //
    {
      int const& v =
        o.Quote_Size;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.Quote_Size_value, is_null, v);
      i.Quote_Size_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // SERIES
    //
    {
      ::std::string const& v =
        o.SERIES;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.SERIES_value,
        sizeof (i.SERIES_value) - 1,
        size,
        is_null,
        v);
      i.SERIES_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // Instrument_Type
    //
    {
      int const& v =
        o.Instrument_Type;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.Instrument_Type_value, is_null, v);
      i.Instrument_Type_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Source
    //
    {
      ::std::string const& v =
        o.Source;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.Source_value,
        sizeof (i.Source_value) - 1,
        size,
        is_null,
        v);
      i.Source_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // TradingCurrency
    //
    {
      int const& v =
        o.TradingCurrency;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.TradingCurrency_value, is_null, v);
      i.TradingCurrency_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // SettlementCurrency
    //
    {
      int const& v =
        o.SettlementCurrency;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.SettlementCurrency_value, is_null, v);
      i.SettlementCurrency_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // TickSizeSlabId
    //
    {
      int const& v =
        o.TickSizeSlabId;

      bool is_null (false);
      mssql::value_traits<
          int,
          mssql::id_int >::set_image (
        i.TickSizeSlabId_value, is_null, v);
      i.TickSizeSlabId_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // UnderlyingEstee_Id
    //
    {
      ::std::string const& v =
        o.UnderlyingEstee_Id;

      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.UnderlyingEstee_Id_value,
        sizeof (i.UnderlyingEstee_Id_value) - 1,
        size,
        is_null,
        v);
      i.UnderlyingEstee_Id_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }

    // AsOfDate
    //
    {
      ::boost::posix_time::ptime const& v =
        o.AsOfDate;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.AsOfDate_value, 3, is_null, v);
      i.AsOfDate_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Last_update_time
    //
    {
      ::boost::posix_time::ptime const& v =
        o.Last_update_time;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.Last_update_time_value, 3, is_null, v);
      i.Last_update_time_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // Expiry_Date
    //
    {
      ::boost::posix_time::ptime const& v =
        o.Expiry_Date;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.Expiry_Date_value, 3, is_null, v);
      i.Expiry_Date_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // SettlementDate
    //
    {
      ::boost::posix_time::ptime const& v =
        o.SettlementDate;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.SettlementDate_value, 3, is_null, v);
      i.SettlementDate_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // TradingStartDate
    //
    {
      ::boost::posix_time::ptime const& v =
        o.TradingStartDate;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.TradingStartDate_value, 3, is_null, v);
      i.TradingStartDate_size_ind = is_null ? SQL_NULL_DATA : 0;
    }

    // LastTradingDate
    //
    {
      ::boost::posix_time::ptime const& v =
        o.LastTradingDate;

      bool is_null (true);
      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_image (
        i.LastTradingDate_value, 3, is_null, v);
      i.LastTradingDate_size_ind = is_null ? SQL_NULL_DATA : 0;
    }
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // Estee_ID
    //
    {
      ::std::string& v =
        o.Estee_ID;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Estee_ID_value,
        static_cast<std::size_t> (i.Estee_ID_size_ind),
        i.Estee_ID_size_ind == SQL_NULL_DATA);
    }

    // DataFeed_Ticker
    //
    {
      ::std::string& v =
        o.DataFeed_Ticker;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.DataFeed_Ticker_value,
        static_cast<std::size_t> (i.DataFeed_Ticker_size_ind),
        i.DataFeed_Ticker_size_ind == SQL_NULL_DATA);
    }

    // Underlying_Ticker
    //
    {
      ::std::string& v =
        o.Underlying_Ticker;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Underlying_Ticker_value,
        static_cast<std::size_t> (i.Underlying_Ticker_size_ind),
        i.Underlying_Ticker_size_ind == SQL_NULL_DATA);
    }

    // ISIN
    //
    {
      ::std::string& v =
        o.ISIN;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.ISIN_value,
        static_cast<std::size_t> (i.ISIN_size_ind),
        i.ISIN_size_ind == SQL_NULL_DATA);
    }

    // Exchange_Code
    //
    {
      ::std::string& v =
        o.Exchange_Code;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Exchange_Code_value,
        static_cast<std::size_t> (i.Exchange_Code_size_ind),
        i.Exchange_Code_size_ind == SQL_NULL_DATA);
    }

    // Security_Type
    //
    {
      int& v =
        o.Security_Type;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.Security_Type_value,
        i.Security_Type_size_ind == SQL_NULL_DATA);
    }

    // Option_Type
    //
    {
      ::std::string& v =
        o.Option_Type;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Option_Type_value,
        static_cast<std::size_t> (i.Option_Type_size_ind),
        i.Option_Type_size_ind == SQL_NULL_DATA);
    }

    // Strike_Price
    //
    {
      double& v =
        o.Strike_Price;

      mssql::value_traits<
          double,
          mssql::id_float8 >::set_value (
        v,
        i.Strike_Price_value,
        i.Strike_Price_size_ind == SQL_NULL_DATA);
    }

    // Tick_Size
    //
    {
      double& v =
        o.Tick_Size;

      mssql::value_traits<
          double,
          mssql::id_float8 >::set_value (
        v,
        i.Tick_Size_value,
        i.Tick_Size_size_ind == SQL_NULL_DATA);
    }

    // Lot_Size
    //
    {
      int& v =
        o.Lot_Size;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.Lot_Size_value,
        i.Lot_Size_size_ind == SQL_NULL_DATA);
    }

    // Status
    //
    {
      ::std::string& v =
        o.Status;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Status_value,
        static_cast<std::size_t> (i.Status_size_ind),
        i.Status_size_ind == SQL_NULL_DATA);
    }

    // Token
    //
    {
      int& v =
        o.Token;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.Token_value,
        i.Token_size_ind == SQL_NULL_DATA);
    }

    // TOKEN2
    //
    {
      ::std::string& v =
        o.TOKEN2;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.TOKEN2_value,
        static_cast<std::size_t> (i.TOKEN2_size_ind),
        i.TOKEN2_size_ind == SQL_NULL_DATA);
    }

    // Expiry_Date2
    //
    {
      ::std::string& v =
        o.Expiry_Date2;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Expiry_Date2_value,
        static_cast<std::size_t> (i.Expiry_Date2_size_ind),
        i.Expiry_Date2_size_ind == SQL_NULL_DATA);
    }

    // Quote_Size
    //
    {
      int& v =
        o.Quote_Size;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.Quote_Size_value,
        i.Quote_Size_size_ind == SQL_NULL_DATA);
    }

    // SERIES
    //
    {
      ::std::string& v =
        o.SERIES;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.SERIES_value,
        static_cast<std::size_t> (i.SERIES_size_ind),
        i.SERIES_size_ind == SQL_NULL_DATA);
    }

    // Instrument_Type
    //
    {
      int& v =
        o.Instrument_Type;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.Instrument_Type_value,
        i.Instrument_Type_size_ind == SQL_NULL_DATA);
    }

    // Source
    //
    {
      ::std::string& v =
        o.Source;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.Source_value,
        static_cast<std::size_t> (i.Source_size_ind),
        i.Source_size_ind == SQL_NULL_DATA);
    }

    // TradingCurrency
    //
    {
      int& v =
        o.TradingCurrency;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.TradingCurrency_value,
        i.TradingCurrency_size_ind == SQL_NULL_DATA);
    }

    // SettlementCurrency
    //
    {
      int& v =
        o.SettlementCurrency;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.SettlementCurrency_value,
        i.SettlementCurrency_size_ind == SQL_NULL_DATA);
    }

    // TickSizeSlabId
    //
    {
      int& v =
        o.TickSizeSlabId;

      mssql::value_traits<
          int,
          mssql::id_int >::set_value (
        v,
        i.TickSizeSlabId_value,
        i.TickSizeSlabId_size_ind == SQL_NULL_DATA);
    }

    // UnderlyingEstee_Id
    //
    {
      ::std::string& v =
        o.UnderlyingEstee_Id;

      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_value (
        v,
        i.UnderlyingEstee_Id_value,
        static_cast<std::size_t> (i.UnderlyingEstee_Id_size_ind),
        i.UnderlyingEstee_Id_size_ind == SQL_NULL_DATA);
    }

    // AsOfDate
    //
    {
      ::boost::posix_time::ptime& v =
        o.AsOfDate;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.AsOfDate_value,
        i.AsOfDate_size_ind == SQL_NULL_DATA);
    }

    // Last_update_time
    //
    {
      ::boost::posix_time::ptime& v =
        o.Last_update_time;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.Last_update_time_value,
        i.Last_update_time_size_ind == SQL_NULL_DATA);
    }

    // Expiry_Date
    //
    {
      ::boost::posix_time::ptime& v =
        o.Expiry_Date;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.Expiry_Date_value,
        i.Expiry_Date_size_ind == SQL_NULL_DATA);
    }

    // SettlementDate
    //
    {
      ::boost::posix_time::ptime& v =
        o.SettlementDate;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.SettlementDate_value,
        i.SettlementDate_size_ind == SQL_NULL_DATA);
    }

    // TradingStartDate
    //
    {
      ::boost::posix_time::ptime& v =
        o.TradingStartDate;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.TradingStartDate_value,
        i.TradingStartDate_size_ind == SQL_NULL_DATA);
    }

    // LastTradingDate
    //
    {
      ::boost::posix_time::ptime& v =
        o.LastTradingDate;

      mssql::value_traits<
          ::boost::posix_time::ptime,
          mssql::id_datetime >::set_value (
        v,
        i.LastTradingDate_value,
        i.LastTradingDate_size_ind == SQL_NULL_DATA);
    }
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      std::size_t size (0);
      mssql::value_traits<
          ::std::string,
          mssql::id_string >::set_image (
        i.id_value,
        sizeof (i.id_value) - 1,
        size,
        is_null,
        id);
      i.id_size_ind =
        is_null ? SQL_NULL_DATA : static_cast<SQLLEN> (size);
    }
  }

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::persist_statement[] =
  "INSERT INTO [EsteeMaster] "
  "([DataFeed_Ticker], "
  "[Underlying_Ticker], "
  "[ISIN], "
  "[Exchange_Code], "
  "[Security_Type], "
  "[Option_Type], "
  "[Strike_Price], "
  "[Tick_Size], "
  "[Lot_Size], "
  "[Status], "
  "[Token], "
  "[TOKEN2], "
  "[Expiry_Date2], "
  "[Quote_Size], "
  "[SERIES], "
  "[Instrument_Type], "
  "[Source], "
  "[TradingCurrency], "
  "[SettlementCurrency], "
  "[TickSizeSlabId], "
  "[UnderlyingEstee_Id], "
  "[AsOfDate], "
  "[Last_update_time], "
  "[Expiry_Date], "
  "[SettlementDate], "
  "[TradingStartDate], "
  "[LastTradingDate]) "
  "OUTPUT INSERTED.[Estee_ID] "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::find_statement[] =
  "SELECT "
  "[EsteeMaster].[Estee_ID], "
  "[EsteeMaster].[DataFeed_Ticker], "
  "[EsteeMaster].[Underlying_Ticker], "
  "[EsteeMaster].[ISIN], "
  "[EsteeMaster].[Exchange_Code], "
  "[EsteeMaster].[Security_Type], "
  "[EsteeMaster].[Option_Type], "
  "[EsteeMaster].[Strike_Price], "
  "[EsteeMaster].[Tick_Size], "
  "[EsteeMaster].[Lot_Size], "
  "[EsteeMaster].[Status], "
  "[EsteeMaster].[Token], "
  "[EsteeMaster].[TOKEN2], "
  "[EsteeMaster].[Expiry_Date2], "
  "[EsteeMaster].[Quote_Size], "
  "[EsteeMaster].[SERIES], "
  "[EsteeMaster].[Instrument_Type], "
  "[EsteeMaster].[Source], "
  "[EsteeMaster].[TradingCurrency], "
  "[EsteeMaster].[SettlementCurrency], "
  "[EsteeMaster].[TickSizeSlabId], "
  "[EsteeMaster].[UnderlyingEstee_Id], "
  "[EsteeMaster].[AsOfDate], "
  "[EsteeMaster].[Last_update_time], "
  "[EsteeMaster].[Expiry_Date], "
  "[EsteeMaster].[SettlementDate], "
  "[EsteeMaster].[TradingStartDate], "
  "[EsteeMaster].[LastTradingDate] "
  "FROM [EsteeMaster] "
  "WHERE [EsteeMaster].[Estee_ID]=?";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::update_statement[] =
  "UPDATE [EsteeMaster] "
  "SET "
  "[DataFeed_Ticker]=?, "
  "[Underlying_Ticker]=?, "
  "[ISIN]=?, "
  "[Exchange_Code]=?, "
  "[Security_Type]=?, "
  "[Option_Type]=?, "
  "[Strike_Price]=?, "
  "[Tick_Size]=?, "
  "[Lot_Size]=?, "
  "[Status]=?, "
  "[Token]=?, "
  "[TOKEN2]=?, "
  "[Expiry_Date2]=?, "
  "[Quote_Size]=?, "
  "[SERIES]=?, "
  "[Instrument_Type]=?, "
  "[Source]=?, "
  "[TradingCurrency]=?, "
  "[SettlementCurrency]=?, "
  "[TickSizeSlabId]=?, "
  "[UnderlyingEstee_Id]=?, "
  "[AsOfDate]=?, "
  "[Last_update_time]=?, "
  "[Expiry_Date]=?, "
  "[SettlementDate]=?, "
  "[TradingStartDate]=?, "
  "[LastTradingDate]=? "
  "WHERE [Estee_ID]=?";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::erase_statement[] =
  "DELETE FROM [EsteeMaster] "
  "WHERE [Estee_ID]=?";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::query_statement[] =
  "SELECT "
  "[EsteeMaster].[Estee_ID], "
  "[EsteeMaster].[DataFeed_Ticker], "
  "[EsteeMaster].[Underlying_Ticker], "
  "[EsteeMaster].[ISIN], "
  "[EsteeMaster].[Exchange_Code], "
  "[EsteeMaster].[Security_Type], "
  "[EsteeMaster].[Option_Type], "
  "[EsteeMaster].[Strike_Price], "
  "[EsteeMaster].[Tick_Size], "
  "[EsteeMaster].[Lot_Size], "
  "[EsteeMaster].[Status], "
  "[EsteeMaster].[Token], "
  "[EsteeMaster].[TOKEN2], "
  "[EsteeMaster].[Expiry_Date2], "
  "[EsteeMaster].[Quote_Size], "
  "[EsteeMaster].[SERIES], "
  "[EsteeMaster].[Instrument_Type], "
  "[EsteeMaster].[Source], "
  "[EsteeMaster].[TradingCurrency], "
  "[EsteeMaster].[SettlementCurrency], "
  "[EsteeMaster].[TickSizeSlabId], "
  "[EsteeMaster].[UnderlyingEstee_Id], "
  "[EsteeMaster].[AsOfDate], "
  "[EsteeMaster].[Last_update_time], "
  "[EsteeMaster].[Expiry_Date], "
  "[EsteeMaster].[SettlementDate], "
  "[EsteeMaster].[TradingStartDate], "
  "[EsteeMaster].[LastTradingDate] "
  "FROM [EsteeMaster]";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::erase_query_statement[] =
  "DELETE FROM [EsteeMaster]";

  const char access::object_traits_impl< ::EsteeMaster, id_mssql >::table_name[] =
  "[EsteeMaster]";

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mssql;

    mssql::connection& conn (
      mssql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    init (im, obj, statement_insert);

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.Estee_ID = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mssql;
    using mssql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mssql::transaction& tr (mssql::transaction::current ());
    mssql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.Estee_ID);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    init (im, obj, statement_update);

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::EsteeMaster, id_mssql >::
  erase (database& db, const id_type& id)
  {
    using namespace mssql;

    ODB_POTENTIALLY_UNUSED (db);

    mssql::connection& conn (
      mssql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::EsteeMaster, id_mssql >::pointer_type
  access::object_traits_impl< ::EsteeMaster, id_mssql >::
  find (database& db, const id_type& id)
  {
    using namespace mssql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mssql::connection& conn (
      mssql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);
    auto_result ar;

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();

      ar.set (sts.find_statement ());
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      st.stream_result ();
      ar.free ();
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::EsteeMaster, id_mssql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mssql;

    mssql::connection& conn (
      mssql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    auto_result ar (st);
    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    st.stream_result ();
    ar.free ();
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::EsteeMaster, id_mssql >::
  reload (database& db, object_type& obj)
  {
    using namespace mssql;

    mssql::connection& conn (
      mssql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.Estee_ID);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    auto_result ar (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    st.stream_result ();
    ar.free ();
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::EsteeMaster, id_mssql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mssql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r != select_statement::no_data)
    {
      ar.release ();
      return true;
    }
    else
      return false;
  }

  result< access::object_traits_impl< ::EsteeMaster, id_mssql >::object_type >
  access::object_traits_impl< ::EsteeMaster, id_mssql >::
  query (database&, const query_base_type& q)
  {
    using namespace mssql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mssql::connection& conn (
      mssql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mssql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::EsteeMaster, id_mssql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mssql;

    mssql::connection& conn (
      mssql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
